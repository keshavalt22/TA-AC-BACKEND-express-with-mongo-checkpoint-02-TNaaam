<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Event Form</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
    <div class="container">
        <header class="flex">
            <a href="/">
                <h2 class="home">Home</h2>
            </a>
            <nav>
                <a href="/events">
                    <button class="btn-green">Events</button>
                </a>
            </nav>
        </header>
        <main>
            <h1 class="heading">Edit Events</h1>
            <form action="/events/<%= event._id %>" method="post" class="form">
                <input type="text" name="title" placeholder="Add Title" value="<%= event.title %>">
                <textarea name="summary" rows="4" placeholder="Add Summary"><%= event.summary %></textarea>
                <input type="text" name="host" placeholder="Add Host" value="<%= event.host %>">
                <input type="date" id="start" name="startDate" value="<%= event.startDate %>">
                <input type="date" id="end" name="endDate" value="<%= event.endDate %>">
                <input type="text" name="location" placeholder="Add Location" value="<%= event.location %>">
                <button type="submit" class="btn-blue">Update Event</button>
            </form>
        </main>
        <footer>
            <small>&copy; AltCampus 2019</small>
        </footer>
    </div>
</body>
</html>